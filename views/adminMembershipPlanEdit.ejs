<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>Edit Plan - Admin</title>
  <style>
    body { background:#f6f8fb; }
    .navbar-admin { background: linear-gradient(90deg, #0d6efd, #34c759); }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-dark navbar-admin mb-3">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/admin/dashboard">Admin</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="adminNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item"><a class="nav-link" href="/admin/dashboard">Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/products">Products</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/promocodes">Promos</a></li>
          <li class="nav-item"><a class="nav-link active" aria-current="page" href="/admin/membership-plans">Membership</a></li>
        </ul>
        <span class="navbar-text text-light me-2">Hello, <%= user && (user.username || user.email) %></span>
        <span class="badge bg-light text-dark">Admin</span>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="card shadow-sm">
      <div class="card-body">
        <h4 class="mb-3">Edit membership plan</h4>
        <form method="POST" action="/admin/membership-plans/<%= plan.id %>">
          <div class="row g-3">
            <div class="col-md-6">
              <label class="form-label">Name</label>
              <input name="name" type="text" class="form-control" value="<%= plan.name %>" required>
            </div>
            <div class="col-md-6">
              <label class="form-label">Points multiplier</label>
              <input name="points_multiplier" type="number" step="0.1" min="0.1" class="form-control" value="<%= plan.points_multiplier %>" required>
            </div>
            <div class="col-md-6">
              <label class="form-label">Annual fee</label>
              <input name="annual_fee" type="number" step="0.01" min="0" class="form-control" value="<%= plan.annual_fee %>">
            </div>
            <div class="col-md-6">
              <label class="form-label">Min annual spend</label>
              <input name="min_annual_spend" type="number" step="0.01" min="0" class="form-control" value="<%= plan.min_annual_spend %>">
            </div>
            <div class="col-12">
              <label class="form-label">Benefits</label>
              <textarea name="benefits" class="form-control" rows="3"><%= plan.benefits || '' %></textarea>
            </div>
            <div class="col-12 form-check">
              <input class="form-check-input" type="checkbox" name="active" id="planActive" value="1" <%= plan.active ? 'checked' : '' %>>
              <label class="form-check-label" for="planActive">Active</label>
            </div>
          </div>
          <div class="mt-3 d-flex gap-2">
            <button type="submit" class="btn btn-primary">Save</button>
            <a href="/admin/membership-plans" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
        <form method="POST" action="/admin/membership-plans/<%= plan.id %>/delete" class="mt-3" onsubmit="return confirm('Delete this plan?');">
          <button type="submit" class="btn btn-outline-danger btn-sm">Delete plan</button>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
